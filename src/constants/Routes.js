const ADMIN = "admin";
const ADMINSYS = "syst";
const CLIENT = "client";

const ROUTES = {
  LOGIN: "/",
  ADMIN_HOME: `${ADMIN}`,
  ADMINSYS_HOME: `${ADMINSYS}`,
  CLIENT_HOME: `${CLIENT}`,
};
export default ROUTES;
